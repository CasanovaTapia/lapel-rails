<%= render "client/navbar" %>
<div class="client-content">
	<div class="wardrobe text-center">
		<div class="row">
				<% if @delivered_orders.length > 0 %>
					<% @delivered_orders.each do |order| %>
						<% order.items.each do |item| %>
							<div class="wardrobe-item col-md-3 text-center">
								<%= link_to image_tag('box.png', class: "img-circle img-item"), item %>
								<h6><%= item.brand %></h6>
								<p><%= item.name %></p>
							</div>
						<% end %>
					<% end %>
				<% else %>
					<h5>Your wardrobe is empty, please submit an order.</h5>
					<%= link_to "BACK", :back %>
				<% end %>
		</div>
	</div>
</div>
